const Joi = require("joi");
// { validationResult }

// module.exports = async (req, res, next) => {
// 	const errors = await validateResult(req.body);
// 	if (errors.isEmpty()) {
// 		return next();
// 	}
// 	return res.status(412).json({ errorMessage: errors });
// };
